services:
  - type: web
    name: oneclick-video-downloader
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "5000"
      - key: FLASK_ENV
        value: production
